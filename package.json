{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "pnpm run build && wrangler dev",
    "postinstall": "nuxt prepare",
    "deploy": "pnpm run build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "migrate": "wrangler d1 migrations apply kine",
    "migrate-remote": "wrangler d1 migrations apply kine --remote",
    "migrate-prev": "wrangler d1 migrations apply kine --prev"
  },
  "dependencies": {
    "nuxt": "^4.1.3",
    "vue": "^3.5.22",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "nitro-cloudflare-dev": "^0.2.2",
    "nitropack": "^2.12.7",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "wrangler": "^4.42.2"
  }
}